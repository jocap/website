#!/bin/sh

read datetime
read title
title=${title#\# }
read meta
text=$(bin/markdown)

content=$(cat <<CONTENT
<article>
<h1 id="title">$title</h1>
${meta:+<p id=\"meta\">$meta</p>}
<div id="text">
$text
</div>
</article>
CONTENT)

export author datetime title meta content
bin/html-page-tpl
