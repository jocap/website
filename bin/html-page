#!/bin/sh

: ${author:=John Ankarström}

read datetime
read title
title=${title#\# }
read meta
text=$(bin/markdown)

content=$(cat <<CONTENT
<h1 id="title">$title</h1>
${meta:+<p id=\"meta\">$meta</p>}
$text
CONTENT)

export author datetime title meta content
bin/template html
